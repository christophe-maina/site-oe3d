# Dossiers lourds / media — hors Git
_deploy/
_versions/
photos et films/

# Le dossier site/ contient des photos lourdes (73 Mo) — on ne pousse que les PDFs des modes d'emploi
site/*
!site/mode d'emploie/
!site/mode d'emploie/**

# (videos/ est versionné : 82 Mo — vidéos HeyGen FAQ multilingues, indispensable au site)

# QR codes d'accès pro (contiennent les tokens personnels — privé)
_qr-acces/

# Fichiers tests temporaires
test_*.png

# macOS
.DS_Store
.AppleDouble
*.icloud

# Generated QR codes for modes d'emploi (regenerable)
_qr-modes/
